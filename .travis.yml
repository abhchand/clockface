language: ruby
sudo: false
rvm:
  - 2.2.2
bundler_args: --without production
before_install:
  - 'echo ''gem: --no-ri --no-rdoc'' > ~/.gemrc'
before_script:
  - 'rake db:setup'
script: bundle exec rspec
services:
  - postgresql
addons:
  postgresql: "9.6"
